Tailor style rules and terminology to your needs
Add API key to .env file (if not using Anthropic model
If not using Anthropic models, import appropriate library and specify model in interface_to_LLM (currently OpenAI library is also imported and legacy models specified but not used)
You can specify temperature in interface_to_LLM.py
To open tool, run interface.py
Styles to be excluded can be specified
